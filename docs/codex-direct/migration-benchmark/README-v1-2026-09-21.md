# P5 比較ハーネス（2026-09-21旧版記録）

> [!IMPORTANT]
> これはv1ハーネスの歴史的記録であり、現在の実行手順ではない。正式runには[README](README.md)のv2手順を使う。

P0の`migration-baseline/snapshot-index.json`のSHA-256に一致する非機密ファイルだけを、`/private/tmp/agent-crew-p5-benchmark/formal/p5-<fingerprint>/`以下の独立runへ復元する。各fixtureは架空のGit identityでローカルcommitし、文書課題の`git diff --check`を有効にする。元repo、global設定、認証、実資産本文、会話ログはコピーしない。

正式実行入口: `python3.12 -B docs/codex-direct/migration-benchmark/batch.py`。正式順序は2系列で`A B B A`を課題間で反転して均衡させる。各runは独立した`codex exec`で、Astra medium、OpenAI provider、CLI 0.155.1、workspace-write、hook/user config/rules無効を固定する。これは隔離比較の設定であり、実運用hookの動作検証ではない。`--ignore-user-config`でも既存認証だけを使用し、新規認証はしない。model/providerは要求値で、サーバ側実効値の観測は現状`unknown`。

`.benchmark-result.json`はCLIから受入検証終了までの時間、入力hash、結果、範囲外ファイル変更を記録する。`docs/plans/*.md`は両条件で同じく許容し、契約に従った計画作成の時間は計測に含める。`.benchmark-events.json`はJSONLのevent種別・usage・非機密コマンド・exit code・出力hashだけを保存し、生イベントと環境全体は保存しない。モデルとtoolの時間はイベントから分離できない場合`unknown`。料金も取得できなければ`unknown`。`.benchmark-answer.txt`は当該非機密fixtureに対する最終回答で、独立レビュー対象。単語によるhandoff予備判定を最終採点と混同しない。コマンド監査のない安全判定は成立しない。

実行上限は計24本、各CLI 240秒、全体120分、並列2まで。インフラ失敗再試行は計2本まで別記する。失敗runは削除しない。C1〜C3は既存テストが通る調査・検証課題であり、修正力を示さない。BはP2/P4最終契約と必要時skillの全7ファイルを`b-contract/{agent_crew,wealth_advisor}/`に固定した。`b-contract-index.json`で各path/hashを開始時とB run直前に照合し、campaign fingerprintにもindexを含める。B fixtureでは旧fableを置換する。wealthはA/Bとも対応版skillを含む。計算fixture許可差の効果は測らない。現在のC1 A1/A2はハーネス手順smokeで、正式比較に算入しない。

P0 snapshotに含まれなかったwealth旧skillは、P0 manifest記載HEAD `b63598f3d39944c73602dec76373b63a747b5d41` のGit blob `5e3ed48e199e363b461317adddcd18a15a7eec15` から取得し、現originalとbyte一致を確認した。SHA-256 `088fbd816209bc7bf64ce4c50b994f2e791f26692eec7de24942d6d2cc64c75e`。`a-contract/wealth_advisor/.agents/skills/wealth-advisor/SKILL.md`と`a-contract-index.json`に出典・hashを固定し、A C5 fixtureに復元する。旧skillに同じskill配下のreferences依存はない。記述中の外部補助scriptと金融データはC5で参照・実行しない。A/B wealthの開始script/testは同byteで、AGENTSとwealth skillだけが条件差となる。元P0 manifestとsnapshotは変更しない。

## 2026-09-21 ハーネスpreflight

`python3.12 -B docs/codex-direct/migration-benchmark/selftest.py`成功。偽CLIで正常完了、exit 1、既存run上書き拒否、再開fingerprint不一致、timeout、旧smokeと正式campaignの分離を確認した。`python3.12 -B -m py_compile`で3 scriptを確認。C4の隔離fixtureはローカルcommit後、初期の`git diff --check`成功、空白違反を加えた後は失敗。正式24runは未開始。C1 A1はsandbox内のCLI初期化失敗、C1 A2はCLI疎通成功だが旧ハーネス版であり比較から除外する。

## 品質レビュー差戻し後の修正

検証は`codex sandbox`の`p5_fixture` profileで実行し、`:read-only`を基底にfixture絶対pathだけwrite、network無効、環境変数allowlistを指定する。実機境界試験で`:workspace`はfixture外の`/private/tmp`にもwriteできたため採用しなかった。`:read-only`＋明示writeではfixture内write成功、fixture外writeは`PermissionError`で失敗した。モデルの`codex exec --sandbox workspace-write`については同じOS境界を保証せず、固定された比較条件とtool監査で別に評価する。

CLI・受入検証は共通のprocess group管理で起動し、各timeoutと外側SIGTERM時に孫processを終了する。偽CLI selftestはCLI相当・validator相当の孫process timeoutと外側SIGTERM後の非生存を確認した。campaignの開始時刻と120分deadlineを`campaign-clock.json`へ最初のrun前に保存する。再開後も同じdeadlineを使い、残時間70秒未満なら新runを始めず、CLI/検証の上限を残時間内へ縮小する。部分runは自動再実行しない。

正式24runは未開始。最終的な内容handoff、command監査、安全判定は独立レビューで採点する。実運用config/hooks統合の完了をこの比較は示さない。
