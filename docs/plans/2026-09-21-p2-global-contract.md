# P2 個人共通作業契約の適用候補

状態: **個人global文書へ適用済み**（2026-09-21）。原本は[`config/codex/AGENTS.md`](../../config/codex/AGENTS.md)。対象は`~/.codex/AGENTS.md`。根拠は[移行再計画](2026-09-21-codex-operating-model-replan.md)の§4・§5・§6・§8。P1権限templateの実設定適用とは別作業。

適用前のglobal本文は日本語指定のみ。直前SHA-256 `df713f48aa4c17a6b32127c83908db7bd745ca1df67d6d0bf2a7e99d84ce3bfe` と既存backupのbytes一致を確認した。以下の本文を既存本文の末尾に1回だけ追記した。適用後SHA-256は`7e8ff6e21b695083fa87cc893d95f5f48948f946992e2281fd279ce61cb958e4`。

```markdown
# グローバル設定

## 言語

常に日本語で回答すること。説明・コメント・コミュニケーションすべて日本語を使う。技術用語やコード識別子は元の形式を維持する。

# 個人共通作業契約

- 説明・コメント・連絡は日本語で行い、技術用語と識別子は元の形式を維持する。
- 依頼の目的に必要な調査・編集・関連テスト・失敗修正は、許可された範囲で自走する。
- 未追跡を含む既存変更を保持し、他者の作業を上書き・破棄しない。
- commit・push・PRなどは依頼された範囲で実施し、既に許可された操作を再確認しない。
- 依頼外の破壊的操作・外部送信・global設定変更は、対象と影響を具体化して確認する。
- 完了時は実ファイル・Git・テスト結果を根拠に、変更・検証・残る制約を報告する。
- repo固有の作業条件と引継ぎ・保存入口は、対象repoのAGENTS.md等の契約に従う。
```

backupは`/private/tmp/codex-global-AGENTS-20260921-mybbzd5q.bak`（mode 0600、適用前bytesと一致）。`~/.codex`親directoryへの一時ファイル作成は権限上できなかったため、ユーザー指定の限定手順で既存ファイルへ直接追記し、flush/fsync後に元本文のprefix、候補1回、全文bytes、適用後hashを確認した。この文書追記はatomic replaceではない。rollbackする場合は適用後hashとの一致を先に確認し、後続変更があれば自動復元せず差分を確認する。Claude設定とP1 composerのatomic方針は変更していない。

意味の点検: 通常開発に一律の委譲、二段レビュー、Vault保存を課さない。依頼済みのcommit・PRや保守変更は再確認を要求せず、未依頼の外部副作用だけを具体化する。repo固有の承認境界と保存先は各repoの契約が決める。
