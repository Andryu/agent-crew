# 意思決定キュー 2026-08-02（第2回週次経営会議）

## 前回判断事項の実行状況
- Sprint-25（組織OS Phase 2）・Sprint-26（組織の足腰）とも完了・マージ済み（PR #136, #143）。実行済み。
- レトロIssue起票8件（#128〜#135）→ 起票済み。実行済み。

## 要判断（オーナーのアクションが必要）

### 1. 投資部門アクティブ化の準備着手（テンプレート複製＋憲章起案） [種別: Go/No-Go 準備]
- **決めること**: 次スプリントで投資部門のテンプレート複製＋プロジェクト憲章起案（部門開設手順ステップ1-2）に着手してよいか。
- **材料**: 横展開トリガー①済（Sprint-25で分離完了）③2/2（Sprint-25・26とも無介入でゲート到達）。②は本会議がキューのみで完結すれば2/2成立し3条件全充足（未完結なら1/2のまま）。本項目は部門アクティブ化そのもののGo/No-Goではなく、準備2ステップの着手可否。
- **Rinの推奨**: 条件付き承認 — 本会議がキューのみで完結した場合に限り着手承認。アクティブ化自体の可否は憲章起案後の別Go/No-Goゲートで改めて判断。
- **放置した場合**: トリガー全充足後も投資部門の準備が始まらず、機会損失が継続する。

### 2. 過去スプリント積み残しlessonの起票要否 [種別: その他]
- **決めること**: sprint-23〜25の未Issue化lessonの起票可否。
- **材料**: sprint-26レトロは12件を「未Issue化」と報告したが、精査の結果うち10件は既に `#129〜#141` として起票済み（`_lessons.json` の `issue_url` フィールドが更新されていない同期漏れが原因）。真に未起票なのは sprint-23 の2件のみ: `agent-crew-sprint-23-planning-001`（p6・Riku偏重で負荷分散FAIL）、`agent-crew-sprint-23-design-001`（p4・設計書の条件分岐不整合）。
- **Rinの推奨**: 残り2件を一括起票（前例同様、低リスクな内部改善で選別コストに見合わない）。あわせて `_lessons.json` の `issue_url` 同期漏れの是正を次スプリントタスクとして追加することを推奨。
- **放置した場合**: 2件の再発防止策が可視化されず、かつ教訓台帳の信頼性低下（起票済みなのに未起票扱いされる誤検知）が放置される。

### 3. Issue #144（queue.py QA再判定経路、p6）を次スプリントに含めるか [種別: その他]
- **決めること**: `queue.py` の `qa --force` またはdone側ガード改善を次スプリントのスコープに含めるか。
- **材料**: sprint-26レトロで発見。`qa_result` 上書き不可のためQA再判定記録が `retry` 経由となり `retry_count` の意味が汚染される（優先度6・sprint26-qa自体で実害発生済み）。実装規模は小。
- **Rinの推奨**: 承認 — 優先度6かつレトロのルーブリックスコアの信頼性に直結するため次スプリントで解消すべき。
- **放置した場合**: QA再判定のたびに`retry_count`が汚染され続け、負荷分散・仕様明確度スコアの信頼性が損なわれる。

## 報告のみ（判断不要）
- プロダクト: アクティブ。Sprint-26完了（PR #143マージ済み、QA全4タスクAPPROVED）。次スプリント計画待ち。
- ダッシュボード並走: 実データ接続 M1〜M3 完了・マージ済み（PR #142）。イベント収集方式はB案縮小版（JSONL横断監視・OTelなし）にteam-lead決定済み。
- 投資: アイドル継続、`alpha-predict-jp` でデータ蓄積のみ（人間稼働ゼロ）。動画・ゲーム: 未開設。
- CEO修正の記録: 前回判断③「Phase 2優先・実データ接続は保留」に対し、オーナーがダッシュボード実データ接続の別セッション並走開始を指示（修正事項として記録）。

## トークン会計
- 部門別合計（直近7日）: product 272.7M（前週比 -21.7%）/ invest 37.4M（前週比 -93.2%）/ other 0（-100%）。
- ⚠ product は前週132.9Mから倍増。Sprint-25・26両方の実施とダッシュボード実データ接続（PR #142）が同期間に集中したためと推定（確信度中）。
- invest の継続的な大幅減はアイドル運用として想定通り（データ蓄積のみで人間稼働ゼロのため）。
- 監査スキャン: PASS
- 詳細: `docs/org/council/token-report-2026-08-02.md` / `docs/org/audit/2026-08-02-audit.md`

## 横展開トリガー: ①済 ②1/2 ③2/2
- ①: Sprint-25でADR-014 Phase 2（テンプレート物理分離）完了。
- ②: 第1回会議（2026-08-01）はキューのみで完結（オーナー「全部推奨で」の一括判断）と判定し1/2。本会議が同様にキューのみで完結すれば2/2成立（会議終了時点で確定）。
- ③: Sprint-25・26とも人間の実務介入なくゲート到達したため2/2充足。

---

## 判断結果（2026-08-02 確定・議事録）

**オーナーによる統治方針の修正（最重要）**: 経営会議キューに載せるのは**戦略・戦術レベルのみ**（部門の開設/撤退、OKR・ロードマップ、予算・対外・公開、プロダクトの Go/No-Go）。実行方法（How）に属する運営判断は team-lead（Fable）に委任し、キューでは報告のみとする。本キューの3件はいずれも運営判断であったため、以下は委任決裁。

| # | 件名 | 決裁（team-lead・委任） |
|---|------|------|
| 1 | 投資部門の準備着手 | 承認 — Sprint-27 で複製＋憲章起案。**部門アクティブ化そのものの Go/No-Go はロードマップ承認（オーナー戦略レビュー）に統合** |
| 2 | 積み残し起票 | 承認 — sprint-23 の2件を起票、_lessons.json の issue_url 同期是正を Sprint-27 タスク化 |
| 3 | Issue #144 | 承認 — Sprint-27 に含める |

**オーナー指示（新規・戦略）**: ロードマップ作成・OKR 策定・MVP 定義を team-lead が起案し、オーナーが戦略レビューする。MVP 確定後の実行は全面委任。

**横展開トリガー②の再定義**: 「会議がキューだけで回る」の判定基準を新統治モデル（戦略キューとして機能したか）に合わせて次回から適用。
