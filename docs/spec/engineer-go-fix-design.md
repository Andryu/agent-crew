# engineer-go 停止バグ対処方針 — 実装設計書

**Issue**: #64
**設計者**: Alex（architect）/ Sprint-15
**参照**: `docs/spec/engineer-go-investigation.md` §6-1（即時適用）

---

## 1. 変更ファイル一覧

| ファイル | 変更内容 |
|---------|---------|
| `.claude/agents/engineer-go.md` | 参照ファイル制限・委譲前チェックリストを追記 |
| `.claude/agents/pm.md` | engineer-go 委譲時チェックリストを追記 |

---

## 2. engineer-go.md への追記内容

### 追記位置

「実装ワークフロー」セクション（`## 実装ワークフロー` の直下、「### タスク着手時」の前）に以下を挿入する。

### 追記テキスト

```markdown
### コンテキスト超過防止ルール（Issue #64）

サブエージェントとして起動される場合、コンテキストウィンドウ超過で無応答停止するリスクがある。
以下の制限を必ず守ること。

**参照ファイル制限**
- Read で参照するファイルは **1 タスクあたり最大 3 件**
- 200 行を超えるファイルは `limit` / `offset` を使って必要箇所のみ取得する
- `Grep` で必要な関数・定義のみ抽出してから Read する

**委譲拒否条件**（自分に該当する場合は Yuki へ報告して分割を要求する）
- complexity が `L` のタスクを受け取った場合 → M × 2 への分割を要求する
- 変更対象ファイルが 4 件以上になる場合 → タスク分割を要求する
- 指示文が 2,000 トークンを超えると推定される場合 → 要約・分割を要求する
```

---

## 3. pm.md への追記内容

### 追記位置

「### Riku への L タスク制限ルール（Issue #78）」セクションの直後に追記する。

### 追記テキスト

```markdown
### engineer-go 委譲前チェックリスト（Issue #64）

Riku（engineer-go）へタスクを委譲する前に以下を確認すること。
**1 項目でも NG の場合はタスクを分割してから委譲する。**

- [ ] 指示文が 2,000 トークン以下か（おおよそ A4 1 ページ程度）
- [ ] 参照させるファイルが 3 件以下か
- [ ] 200 行を超えるファイルを丸ごと参照させていないか
- [ ] complexity が M 以下か（L タスクは M × 2 に分割済みか）
- [ ] 変更対象ファイルが 3 件以下か
```

---

## 4. 実装時の注意

- pm.md の追記位置は `Riku への L タスク制限ルール` セクション末尾の直後。そのセクションの `---` 区切り線の後に追加する。
- engineer-go.md の追記位置は `## 実装ワークフロー` ヘッダの直後、`### タスク着手時` の前。
- 両ファイルとも既存セクションの削除・変更は行わない。追記のみ。
- 実装完了後に Issue #64 を `gh issue close 64` でクローズする。
