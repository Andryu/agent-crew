# agent-crew 共通作業契約

このファイルは Claude Code と Codex が同じリポジトリで従う共通規約である。ランタイム固有の設定は `.claude/` と `.codex/` に置く。

## 基本原則

- コミュニケーション、説明、コメントは日本語で書く。コード識別子と外部コマンドは原表記を維持する。
- repo、Git、テスト結果が真実であり、会話履歴やhandoffは補助情報である。
- dirty worktreeには他者・他セッションの変更があり得る。無関係な差分を戻さず、対象外ファイルを変更しない。
- 変更前に対象ファイルと既存テストを確認し、変更後に最小限で関連する検証を実行する。
- ファイル列挙、件数、存在確認は `rg`、`fd`、`jq`、Git、テストなどの決定的コマンドで確認する。

## 実装とレビュー

- 曖昧さが安全性、外部副作用、受入仕様を左右する場合は、勝手に解釈せず報告して停止する。
- 実装完了時は、変更ファイル、実行した検証コマンドと生出力、仕様逸脱の有無を報告する。
- 実装とレビューは可能な限り別コンテキストで行う。レビューは仕様準拠と品質を分ける。
- スコープ外のリファクタリング、依存更新、フォーマット全体適用、Git履歴改変を行わない。

## handoff

- ハーネスを交代するときは会話履歴ではなく、目的、現在地、次の一手、未決事項、検証方法、落とし穴とEvidenceを渡す。
- handoffとGitまたはテスト結果が食い違う場合は、後者を優先する。
- quota、認証、ネットワーク、timeoutを同じ失敗として扱わない。自動切替は明示的に有効化されるまで行わない。
